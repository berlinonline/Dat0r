<?php

namespace {{ namespace }};

use {{ parent_implementor }} as Base{{ parent_class_name }};

/**
{% for line in description %}
 * {{ line }}
{% endfor %}
 *
 * This class reflects the declared structure of the '{{ module_name }}'
 * entity. It contains the metadata necessary to initiate and manage the
 * lifecycle of '{{ module_name }}Document' instances. Most importantly
 * it holds a collection of fields (and default fields) that each of the
 * documents of this module supports.
 *
 * For more information and hooks have a look at the base classes.
 */
class {{ class_name }} extends Base{{ parent_class_name }}
{
    //public function getDefaultFields()
    //{
    //    $fields = parent::getDefaultFields();
    //    $fields['language'] = \Your\Custom\LanguageField::create('language', array('default' => 'en_UK'));
    //    $fields['foobar'] = \Your\Custom\FooBarField::create('foobar');
    //    return $fields;
    //}
    //
    //protected function getDocumentImplementor()
    //{
    //    return '\\Your\\Custom\\Document';
    //}
}
