<?php
/*              AUTOGENERATED CODE - DO NOT EDIT !
This base class was generated by the Dat0r library (https://github.com/berlinonline/Dat0r) 
on {{ datetime }} and is closed to modifications by any meaning.
If you are looking for a place to alter the behaviour of '{{ module_name }}' documents,
then the '{{ module_name }}Document' (skeleton) class probally might be a good place to look. */

namespace {{ namespace }};

/**
 * Serves as the base class to the '{{ module_name }}' document skeleton.
 */
abstract class {{ implementor }} extends {{ parent_class }}
{ {% for field in fields %}

    /**
     * Returns an '{{ module_name }}' document's {{ field.name }}.
     *
     * @return {{ field.php_type }}
     */
    public function get{{ field.name|capitalize }}()
    {
        return $this->getValue('{{ field.name }}');
    }

    /**
     * Sets an '{{ module_name }}' document's {{ field.name }}.
     *
     * @param {{ field.php_type }} ${{ field.name }}
     */
    public function set{{ field.name|capitalize }}(${{ field.name }})
    {
        $this->setValue('{{ field.name }}', ${{ field.name }});
    }
{% endfor %}
}
